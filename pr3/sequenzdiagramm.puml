@startuml
title Registrierung und Nutzung eines Roboters

participant "Robot-Node" as Robot
participant "Terminal-Client" as Client
participant "Registry-Server" as Registry

== Registrierung des Roboters ==
Robot -> Registry : register(name, ip, port, type="robot")
Registry -> Registry : prüfe Name
Registry -> Registry : ID vergeben\nEintrag speichern
Registry --> Robot : status=ok, id

== Registrierung des Clients ==
Client -> Registry : register(name, ip, port, type="client")
Registry -> Registry : prüfe Name
Registry -> Registry : ID vergeben\nEintrag speichern
Registry --> Client : status=ok, id

== Abfrage verfügbarer Roboter ==
Client -> Registry : list(type="robot")
Registry --> Client : Liste aller Roboter\n(id, name, ip, port)

== Direkte Robotersteuerung ==
Client -> Robot : TCP-Verbindung aufbauen
Client -> Robot : Steuerbefehl(e)
Robot --> Client : Bestätigung / Status

@enduml
